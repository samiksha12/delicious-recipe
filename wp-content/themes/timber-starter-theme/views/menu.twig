{% if menu %}
	
	{% for item in items %}
		<div class="{{ item.classes | join(' ') }}">
			<a target="{{ item.target }}" href="{{ item.link }}">{{ item.title }}</a><span>|</span>
			{% include "menu.twig" with {'items': item.children} %}
		</div>
	{% endfor %}
	
{% endif %}
